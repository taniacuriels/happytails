{
  "name": "happytails",
  "version": "0.1.0",
  "description": "Shelter Communication Platform for coordinating animal rescue and adoption",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "build:packages": "npm run build --workspace=@happytails/auth --workspace=@happytails/domain --workspace=@happytails/design-tokens --workspace=@happytails/messaging --workspace=@happytails/scheduling --workspace=@happytails/notifications",
    "test": "npm run test --workspaces",
    "test:watch": "npm run test:watch --workspaces",
    "lint": "npm run lint --workspaces",
    "type-check": "npm run type-check --workspaces"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
